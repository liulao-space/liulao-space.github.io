# 案例

演示分两类：

- **在线演示（A 级）**：零依赖单文件，浏览器直接点开看效果 → 在 [日志首页](/vibe-coding/) 的「在线演示」区
- **工程案例（B 级）**：真实工具链，需要 clone 到本地 install 运行 → 见下表

| 案例 | 对应日志 | 一句话 |
|------|----------|--------|
| [mini-monorepo](/examples/mini-monorepo) | [从单仓 SPA 到 pnpm Monorepo](/vibe-coding/2026-08-05-pnpm-monorepo-from-spa) | pnpm workspace 最小闭环：admin + www + 共享 UnoCSS |

以后新日志若有可运行示例，会继续加在对应分区。
